# Bathhouse Finance Section

## Goal
Add a new investor-facing finance section to `/bathhouse` using the figures from `баня_доходы.docx`.

## Tasks
- [x] Update `BathhouseLanding.tsx` with finance data structures for KPI, expenses, revenue mix, and staffing logic. → Verify: the file contains a new finance data block with no dead data.
- [x] Add a new final `Экономика банного комплекса` section after the current `Vision / Investor Note` block. → Verify: `/bathhouse` shows a separate finance section below the existing ending section.
- [x] Keep the section visually aligned with the rest of the site and readable on desktop and mobile. → Verify: the layout collapses cleanly in responsive classes without overflow.
- [x] Run a production build. → Verify: `npm run build` exits successfully.

## Done When
- [x] `/bathhouse` contains a polished finance section with expenses, revenue structure, and profit framing.
- [x] The page still builds and is ready for deploy.
